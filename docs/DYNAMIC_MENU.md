# 动态菜单系统文档

## 📋 功能概览

已实现基于 **menus API** 的动态菜单加载系统，所有组件均使用 **shadcn/ui** 构建。

## ✨ 核心特性

### 1. **动态菜单加载**
- ✅ 从 `/admin/menus` API 自动加载菜单配置
- ✅ 支持主菜单 (main) 和其他菜单 (others) 分组
- ✅ 失败时自动使用默认菜单作为降级方案

### 2. **子菜单支持**
- ✅ 支持多级菜单展开/收起
- ✅ 平滑的展开动画效果
- ✅ 智能记忆展开状态

### 3. **图标系统**
- ✅ 使用 **lucide-react** 图标库
- ✅ 动态加载图标组件
- ✅ 图标名称直接映射（如 "Home" → Home 图标）

### 4. **标签系统**
- ✅ `new` 标签 - 显示绿色 "NEW" 徽章
- ✅ `pro` 标签 - 显示灰色 "PRO" 徽章
- ✅ 自动定位在菜单项右侧

### 5. **侧边栏功能**
- ✅ 支持展开/收起切换
- ✅ 收起时显示图标，展开时显示完整菜单
- ✅ 平滑的过渡动画
- ✅ 响应式布局

### 6. **路由高亮**
- ✅ 自动高亮当前激活的菜单项
- ✅ 父菜单激活时子菜单也会高亮
- ✅ 精确匹配路径

## 📦 已使用的 shadcn/ui 组件

| 组件 | 用途 | 文件 |
|------|------|------|
| `Collapsible` | 子菜单展开/收起 | `collapsible.tsx` |
| `ScrollArea` | 菜单滚动区域 | `scroll-area.tsx` |
| `Badge` | NEW/PRO 标签 | `badge.tsx` |
| `Button` | 按钮组件 | 封装为 `AntButton` |
| `Table` | 表格组件 | 封装为 `AntTable` |
| `Select` | 选择器 | 封装为 `AntSelect` |
| `Dialog` | 对话框 | 封装为 `AntModal` |
| `Input` | 输入框 | 封装为 `AntInput` |

## 🔧 API 接口规范

### Menu API 响应格式

```typescript
interface MenuConfig {
  main: MenuItem[];      // 主菜单
  others: MenuItem[];    // 其他菜单
}

interface MenuItem {
  name: string;          // 菜单名称
  icon: string;          // 图标名称（lucide-react）
  path?: string;         // 路由路径（可选）
  subItems?: SubMenuItem[];  // 子菜单（可选）
}

interface SubMenuItem {
  name: string;          // 子菜单名称
  path: string;          // 路由路径
  pro?: boolean;         // PRO 标记
  new?: boolean;         // NEW 标记
}
```

### API 示例响应

```json
{
  "main": [
    {
      "name": "首页",
      "icon": "Home",
      "path": "/"
    },
    {
      "name": "用户管理",
      "icon": "Users",
      "subItems": [
        {
          "name": "用户列表",
          "path": "/users",
          "new": true
        },
        {
          "name": "用户分组",
          "path": "/users/groups",
          "pro": true
        }
      ]
    }
  ],
  "others": [
    {
      "name": "组件示例",
      "icon": "Component",
      "path": "/demo"
    }
  ]
}
```

## 🎨 支持的图标

可以使用 [lucide-react](https://lucide.dev/icons) 的所有图标，常用图标包括：

- `Home` - 首页
- `Users` - 用户
- `Globe` - 域名/全球
- `Route` - 路由
- `FileText` - 文件/文档
- `Settings` - 设置
- `Bell` - 通知
- `Component` - 组件
- `Database` - 数据库
- `Shield` - 安全
- `Layout` - 布局
- `Package` - 包/模块
- `Folder` - 文件夹
- `File` - 文件
- `Image` - 图片
- `Video` - 视频
- `Music` - 音乐
- `Download` - 下载
- `Upload` - 上传
- `Search` - 搜索
- `Filter` - 筛选
- `Plus` - 添加
- `Minus` - 减少
- `Edit` - 编辑
- `Trash` - 删除
- `Copy` - 复制
- `Check` - 确认
- `X` - 关闭
- `AlertCircle` - 警告
- `Info` - 信息
- `HelpCircle` - 帮助
- `ChevronRight` - 右箭头
- `ChevronDown` - 下箭头

## 🏗️ 组件架构

### 1. DynamicMenu 组件

**位置**: `src/components/DynamicMenu.tsx`

**功能**:
- 渲染菜单项列表
- 处理子菜单展开/收起
- 路由激活状态判断
- 图标动态加载
- 标签徽章显示

**Props**:
```typescript
interface DynamicMenuProps {
  items: MenuItem[];      // 菜单项数组
  sidebarOpen: boolean;   // 侧边栏是否展开
  title?: string;         // 菜单组标题
}
```

### 2. 布局组件

**位置**: `src/app/(admin)/layout.tsx`

**功能**:
- 加载菜单配置
- 渲染侧边栏
- 渲染顶部导航栏
- 内容区域管理

**状态管理**:
- `sidebarOpen` - 侧边栏展开状态
- `menuConfig` - 菜单配置数据
- `loading` - 加载状态

## 📖 使用示例

### 基础菜单

```typescript
const menuConfig: MenuConfig = {
  main: [
    {
      name: "首页",
      icon: "Home",
      path: "/"
    },
    {
      name: "用户管理",
      icon: "Users",
      path: "/users"
    }
  ],
  others: []
}
```

### 带子菜单

```typescript
const menuConfig: MenuConfig = {
  main: [
    {
      name: "系统管理",
      icon: "Settings",
      subItems: [
        {
          name: "用户管理",
          path: "/system/users"
        },
        {
          name: "角色管理",
          path: "/system/roles",
          pro: true
        }
      ]
    }
  ],
  others: []
}
```

### 带标签

```typescript
const menuConfig: MenuConfig = {
  main: [
    {
      name: "功能模块",
      icon: "Package",
      subItems: [
        {
          name: "新功能",
          path: "/features/new",
          new: true        // 显示 NEW 标签
        },
        {
          name: "高级功能",
          path: "/features/pro",
          pro: true        // 显示 PRO 标签
        }
      ]
    }
  ],
  others: []
}
```

## 🎯 布局特性

### 侧边栏布局

```
┌─────────────────┐
│ Logo + Toggle   │ ← 头部（固定高度 64px）
├─────────────────┤
│                 │
│   Menu Items    │ ← 菜单区域（可滚动）
│                 │
├─────────────────┤
│  User Info      │ ← 底部用户信息（固定）
└─────────────────┘
```

### 主内容区布局

```
┌─────────────────────────────────────┐
│  Header (Logo + Actions)            │ ← 顶部栏（固定高度 64px）
├─────────────────────────────────────┤
│                                     │
│        Content Area                 │ ← 内容区域（可滚动）
│                                     │
└─────────────────────────────────────┘
```

## 🚀 启动测试

```bash
# 启动开发服务器
npm run dev

# 访问应用
http://localhost:3003
```

## 🔄 默认菜单

当 API 加载失败时，系统会使用以下默认菜单：

```typescript
{
  main: [
    { name: '首页', icon: 'Home', path: '/' },
    { name: '用户管理', icon: 'Users', path: '/users' },
    { name: '域名管理', icon: 'Globe', path: '/domains' },
    { name: '路由管理', icon: 'Route', path: '/routes' },
    { name: '模板管理', icon: 'FileText', path: '/templates' },
  ],
  others: [
    { name: '组件示例', icon: 'Component', path: '/demo' },
  ]
}
```

## 🎨 样式定制

### 菜单项激活状态

```css
/* 激活的菜单项 */
.bg-accent text-accent-foreground

/* 悬停状态 */
.hover:bg-accent hover:text-accent-foreground
```

### 侧边栏宽度

- 展开: `w-64` (256px)
- 收起: `w-20` (80px)

### 动画效果

```css
/* 侧边栏展开/收起 */
transition-all duration-300

/* 子菜单展开 */
Collapsible 自带动画
```

## 📝 待优化项

1. **权限控制**: 根据用户角色动态过滤菜单项
2. **菜单搜索**: 添加菜单搜索功能
3. **收藏夹**: 支持菜单项收藏
4. **最近访问**: 显示最近访问的菜单
5. **自定义图标**: 支持自定义 SVG 图标
6. **国际化**: 菜单名称多语言支持

---

**动态菜单系统已完成！** 🎉
现在菜单会自动从 API 加载，支持子菜单、图标、标签等完整功能。
